{
  "name": "local-router",
  "version": "0.1.0",
  "description": "A small local OpenAI account router with safe defaults.",
  "type": "module",
  "bin": {
    "local-router": "./src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
